using GameFramework;
using GameFramework.Resource;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;
using UnityGameFramework.Runtime;

public static class ResourceExtention
{
    //public static void LoadBytes(this ResourceComponent resComponent,string abName, LoadBytesCallbacks callback, object userdata)
    //{
    //    if (resComponent.ResourceMode == ResourceMode.Updatable)
    //    {
    //        resComponent.LoadBytes(abName, callback, userdata);
    //    }
    //    else if (resComponent.ResourceMode == ResourceMode.Package)
    //    {
    //        resComponent.LoadBytes(abName, callback, userdata);
    //    }
    //}

}
